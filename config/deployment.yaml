---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tw-api
  name: tw-api
spec:
  minReadySeconds: 5
  replicas: 4
  selector:
    matchLabels:
      app: tw-api
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: tw-api
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: api-config
        image: <IMAGE>
        name: tw-api
        imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: tw-api-service
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: tw-api
  type: LoadBalancer